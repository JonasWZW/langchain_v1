"""
human-in-the-loop

人机交互中间件 (HITL) 允许您在代理工具调用中添加人工监督。
当模型提出可能需要审核的操作（例如，写入文件或执行 SQL）时，中间件可以暂停执行并等待决策。

它通过检查每个工具调用是否符合可配置的策略来实现这一点。
如果需要干预，中间件会发出中断以暂停执行。图状态使用 LangGraph 的持久层保存，因此执行可以安全地暂停并在稍后恢复。
然后，由人来决定接下来会发生什么：该操作可以按原样批准（ approve ），在运行之前进行修改（ edit ），或者拒绝并给予反馈（ reject ）。



每个工具可用的决策类型取决于您在 interrupt_on 中配置的策略。
当多个工具调用同时暂停时，每个操作都需要单独的决策。决策必须按照操作在中断请求中出现的顺序提供。


"""